

services:
  backend:
    build:
      context: ./backend
    ports:
      - "8081:8081"
    environment:
      DB_HOST: db
      DB_USER: root
      DB_PASSWORD: qwert
      DB_NAME: tareas
  frontend:
    build:
      context: ./frontend/react-app
    ports:
      - "5173:5173"
    depends_on:
      - backend


volumes:
  db_data:
